@model IEnumerable<FrostbyteWebApp.Models.UserBank>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BankName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BranchCodeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BankName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BranchCodeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserBankID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserBankID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserBankID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
