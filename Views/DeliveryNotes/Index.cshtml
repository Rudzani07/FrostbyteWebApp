@model IEnumerable<FrostbyteWebApp.Models.DeliveryNote>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateIssued)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.S)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateIssued)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.S.Address1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeliveryNoteID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeliveryNoteID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeliveryNoteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
