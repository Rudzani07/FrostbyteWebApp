@model IEnumerable<FrostbyteWebApp.Models.PmRequestForQoutation>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PurcasingManagerID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PR.ItemCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PurcasingManagerID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PmRequestForQoutationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PmRequestForQoutationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PmRequestForQoutationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
